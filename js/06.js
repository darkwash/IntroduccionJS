// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.1); //ceil siempre redondea hacia arriba
resultado = Math.floor(7.0); //floor siempre redondea hacia abajo
resultado = Math.sqrt(144); // sqrt Calcula raiz cuadrada
resultado = Math.abs(-200); //abs cambia el valor a positivo
resultado = Math.min(3, 5, 1, 8, 2, 10); //min Muestra el valor mas chico en un numero
resultado = Math.max(3, 5, 1, 8, 2, 10); //max Muestra el valor mas grande en un numero
resultado = Math.random();
resultado = Math.floor(Math.random() * 30);

console.log(resultado);